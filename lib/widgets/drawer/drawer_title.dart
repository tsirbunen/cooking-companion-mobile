import 'package:flutter/material.dart';

class DrawerTitle extends StatelessWidget {
  const DrawerTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('CONTENTS');
  }
}
